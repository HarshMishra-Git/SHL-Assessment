[phases.setup]
nixPkgs = ['python310']

[phases.install]
cmds = ['pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt']

[start]
cmd = 'uvicorn api.main:app --host 0.0.0.0 --port $PORT'
